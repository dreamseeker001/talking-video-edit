---
name: talking-video-edit
metadata:
  version: "1.1"
description: 使用本地 OpenEdit、FFmpeg 和 WhisperX 剪辑中文口播，完成失误清理、校色、演示编排、字幕与成片；支持安装初始化、归档参考、恢复偏好及同片修改。用于实际剪片或搭建这套工作流，单纯讨论工具不触发安装或渲染。
---

# 口播剪辑

[English translation](docs/en/skill.md)。按需阅读一种语言版本，不同时加载两份。

面向 Windows x64。使用现成工具及 OpenEdit 原生工程，不创建剪辑器或通用编排平台。

## 三种入口

- 安装、首次使用或依赖损坏：读 [setup.md](references/setup.md)，执行 `scripts/Install-VideoSkill.ps1`。安装任务当场执行准备，不只复制目录。偏好未确认时再读 [preferences.md](references/preferences.md)，沿用已有回答，只补问缺项。
- 日常新片或续做：执行 `scripts/Prepare-VideoTask.ps1 -Media <原片> -RunKey <唯一英文键>`，续做可传 `-RunDir <原生项目目录>`。读取它报告的总偏好、项目快照和 task.md，实际查看活跃参考关键帧。健康环境不读安装历史。
- 日常编排与异常恢复读 [agent-recovery.md](references/agent-recovery.md)。`Run-VideoTask.ps1` 是分阶段辅助入口；返回 Agent 待处理状态后同一任务内继续，不向用户逐步索要确认。
- 修改风格、归档参考或反馈记忆：按需读 [preferences.md](references/preferences.md)。本次指令优先；旧片快照不随全局更新而静默改变。

每次发动都必须完整读取总偏好，而不是只读取 JSON 摘要：摘要决定机器参数，正文决定适用范围、采纳/舍弃边界和冲突解释。读取后先执行一次偏好审计：确认当前 profile、参考、输出规格和本片覆盖都有明确来源；缺少可执行映射时先补齐或停在渲染前，不得静默退回通用模板。`styleId`、revision、模块路径和 profile 名是内部执行标识，不是用户必须记住或命名的风格。历史工程中的 V7/V8 只能作为证据索引，不能变成代码中的特殊分支。用户直接在沟通框说明新片、修改或“记住这个”即可，不要求输入命令或代码。

本机资料位置由 `scripts/local-settings.json` 指向；长期偏好、环境和参考在 skill 外，更新 skill 不覆盖它们。复制 skill 到新机器不能沿用旧机器的路径。

## 日常六阶段

1. Prepare恢复完整偏好、冻结合同、参考、素材和task；复用有效ASR，脚本辅助意图，字幕依据口播。
2. Agent通读全片后统一决定保留/删除、画面叙事、字幕短语、卡片与校色策略，写final全片计划。首次新编排读 [editing.md](references/editing.md)。不先机械裁剪再补语义。
3. 从计划派生原生EDL，执行Cut和原生重定时；核对保留词句，投影到剪后计划。工具不能可靠映射的部分由Agent兜底，不另造时间轴。
4. Agent按冻结合同完成底片校色/合成/规格化，实际重组字幕chunks并核对完整短语、时长和字数。冻结最终转录及计划后Prep，再派生presentation与policy-review；不沿用过期注释，不把待执行策略标为完成。
5. 预检通过后Render、Check。技术异常由Agent定位并从最近有效阶段恢复，不把普通修复交给用户确认；无法在授权范围内解决才询问。不得为过检削减已确认风格。
6. Agent检查实际画面及有能力验证的声音，交付可播放成片，更新task.md。若有兜底，最终必须汇报动作、验证、限制及完善建议；没有听辨不声称已听过。

脚本与Agent在上述边界内共同完成即为正常完成，不追求脚本独立完成。正常语义/视觉判断与异常兜底区分记录，细则见 [agent-recovery.md](references/agent-recovery.md)。

## 必须保留的边界

- 原片不覆盖；保留原生 EDL、逐字转录、配方副本和最近可接受版本。不改上游配方或生成的 `.wv`。正式任务使用当前 profile 冻结的模块和 adapter；`captions-clean.ts` 只作环境基线。
- 当前帧率 59.94 经引擎可能变 59；实际检查输出，必要时显式转 60 并记录，不能假称完全同帧率。默认画幅、分辨率、音轨由总偏好决定。
- 曝光/白平衡逐片判断；LUT 有明确素材适配才用，不对字幕和卡片整体调色。演示音轨与旁白分开处理。
- 当前引擎不支持 `-webkit-text-stroke`；中文粗描边不用八方向硬阴影拼接。黑底字幕仍需实际视觉核验居中。
- 本地生成普通字幕/说明动画；复杂新镜头写提示词交用户外部生成。不得自动调用付费服务、购买订阅或伪造产品 UI。
- 用户提出“以后都这样”“记住这个风格/规则”等明确长期要求时，必须启动偏好更新流程；一次局部修改不能静默升级为全局规则。反复出现且可能跨片复用的修改，先作为候选并询问是否蒸馏。更新通过 `Update-VideoPreferences.ps1` 进行，写入后完整复读。
- 每次写入偏好后重新读取完整文件并检查冲突。存在互相矛盾的全局规则时，暂停需要依赖冲突项的渲染，向用户提供按场景细化或二选一的选择；不能用文件后写入的内容覆盖前一条而假装解决。
- 日常不重复安装、研究工具、读取全量历史或重新分析整部参考。需要复核动作/节奏、证据冲突或用户换参考时才回看对应原片段。
